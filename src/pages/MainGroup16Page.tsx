import main from './subpages/_GEN_main16';
import MainGroupBasic from './MainGroupBasic';

const MainGroup16Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup16Page;
