import main from './subpages/__main4';
import MainGroupBasic from './MainGroupBasic';

const MainGroup04Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup04Page;
