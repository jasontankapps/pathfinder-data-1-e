import main from './subpages/__main20';
import MainGroupBasic from './MainGroupBasic';

const MainGroup20Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup20Page;
