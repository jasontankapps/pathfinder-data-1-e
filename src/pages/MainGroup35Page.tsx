import main from './subpages/__main35';
import MainGroupBasic from './MainGroupBasic';

const MainGroup35Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup35Page;
