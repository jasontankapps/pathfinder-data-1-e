import { GroupProps, BasicGroupPage } from './BasicGroupPage';

const ClassAbilityGroupBasic = (props: GroupProps) =>
	<BasicGroupPage {...props} prefix="ability" maybeTopLink={null} />;

export default ClassAbilityGroupBasic;
