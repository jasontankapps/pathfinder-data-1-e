import main from './subpages/__main33';
import MainGroupBasic from './MainGroupBasic';

const MainGroup33Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup33Page;
