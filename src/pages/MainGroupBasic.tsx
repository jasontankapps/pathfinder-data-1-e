import { GroupProps, BasicGroupPage } from './BasicGroupPage';

const MainGroupBasic = (props: GroupProps) =>
	<BasicGroupPage {...props} prefix="main" className="main" />;

export default MainGroupBasic;
