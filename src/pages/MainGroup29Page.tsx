import main from './subpages/__main29';
import MainGroupBasic from './MainGroupBasic';

const MainGroup29Page: React.FC<{id: string}> = ({id}) => <MainGroupBasic id={id} info={main} />;

export default MainGroup29Page;
